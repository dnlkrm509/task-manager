import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const FlaggedEmailScreen = () => {
    return (
        <View>
            <Text>Flagged Email Screen</Text>
        </View>
    )
};

const style = StyleSheet.create();

export default FlaggedEmailScreen;